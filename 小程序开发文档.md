# 智能盲杖小程序端开发文档

## 项目概述

智能盲杖小程序端是面向监护人/家属的移动端应用，用于实时监控和管理视障老人使用智能盲杖的情况。小程序端与后端系统对接，提供便捷的移动端管理界面，让监护人能够随时了解老人的位置、状态和安全情况。

## 技术选型

### 前端框架
- **框架**: Uni-app（支持多端）
- **UI组件库**:  uView UI
- **状态管理**:  Pinia
- **地图组件**: 微信小程序地图组件 / 高德地图小程序SDK
- **图表组件**: ECharts for WeChat Mini Program

### 后端对接
- **API接口**: RESTful API
- **认证方式**: JWT Token
- **实时通信**: WebSocket（用于实时报警推送）
- **数据格式**: JSON

## 功能模块设计

### 1. 用户认证模块

#### 1.1 登录/注册
- **手机号登录**: 通过手机号+验证码登录
- **微信授权登录**: 使用微信一键登录
- **监护人注册**: 新监护人账号注册
- **忘记密码**: 手机号验证码重置密码

#### 1.2 用户信息
- **个人信息管理**: 修改头像、昵称、手机号
- **实名认证**: 上传身份证照片进行实名认证
- **密码修改**: 修改登录密码

### 2. 设备绑定模块

#### 2.1 设备管理
- **设备绑定**: 扫描设备二维码绑定盲杖设备
- **设备列表**: 查看已绑定的设备列表
- **设备解绑**: 解绑不再使用的设备
- **设备信息**: 查看设备详细信息（设备ID、设备名称、绑定时间等）

#### 2.2 用户关联
- **绑定老人**: 绑定视障老人信息
- **老人信息管理**: 编辑老人基本信息（姓名、年龄、照片、联系方式等）
- **多老人管理**: 一个监护人可绑定多个老人
- **监护人共享**: 多个监护人可绑定同一个老人

### 3. 实时监控模块

#### 3.1 实时位置
- **实时地图**: 显示老人当前位置
- **位置刷新**: 定时刷新老人位置（可配置刷新间隔）
- **位置精度**: 显示定位精度和GPS信号强度
- **地址解析**: 显示当前位置的详细地址

#### 3.2 设备状态
- **在线状态**: 显示设备在线/离线状态
- **电池电量**: 显示设备剩余电量
- **设备健康度**: 显示设备整体健康状态
- **最后更新时间**: 显示最后数据更新时间

#### 3.3 传感器数据
- **障碍物距离**: 实时显示障碍物距离
- **环境数据**: 显示当前温度、湿度
- **运动状态**: 显示老人运动状态（静止、行走、跑步等）

### 4. 报警管理模块

#### 4.1 实时报警
- **报警推送**: 实时接收报警通知（跌倒、越界、低电量等）
- **报警详情**: 查看报警详细信息（时间、位置、类型、现场数据）
- **报警处理**: 标记报警为已处理/忽略
- **紧急联系**: 一键拨打老人电话或紧急联系人电话

#### 4.2 报警历史
- **报警记录**: 查看历史报警记录列表
- **报警筛选**: 按时间、类型、状态筛选报警
- **报警统计**: 统计各类报警数量和趋势
- **报警导出**: 导出报警记录（可选）

#### 4.3 报警类型
- **跌倒报警**: 检测到老人跌倒
- **越界报警**: 老人离开电子围栏区域
- **低电量报警**: 设备电量低于阈值
- **长时间静止报警**: 老人长时间无移动
- **障碍物报警**: 障碍物距离过近

### 5. 电子围栏模块

#### 5.1 围栏管理
- **围栏列表**: 查看已设置的电子围栏
- **创建围栏**: 创建新的电子围栏（圆形、矩形、多边形）
- **编辑围栏**: 修改围栏范围和参数
- **删除围栏**: 删除不再使用的围栏

#### 5.2 围栏设置
- **围栏类型**: 圆形围栏（半径设置）、矩形围栏（边界设置）
- **围栏名称**: 自定义围栏名称（如：家、公园、医院）
- **报警开关**: 开启/关闭围栏越界报警
- **时间设置**: 设置围栏生效时间段（如：仅白天生效）

#### 5.3 围栏监控
- **围栏状态**: 显示老人是否在围栏内
- **越界提醒**: 老人离开围栏时立即提醒
- **围栏历史**: 查看历史越界记录

### 6. 轨迹回放模块

#### 6.1 轨迹查询
- **时间选择**: 选择查询的时间范围
- **轨迹加载**: 加载指定时间段的运动轨迹
- **轨迹播放**: 播放轨迹动画
- **播放速度**: 调整播放速度（慢速、正常、快速）

#### 6.2 轨迹分析
- **轨迹统计**: 统计运动距离、停留时间、移动速度
- **停留点标记**: 标记长时间停留的位置
- **轨迹导出**: 导出轨迹数据（可选）

### 7. 数据统计模块

#### 7.1 活动统计
- **今日步数**: 显示今日行走步数
- **活动时长**: 显示今日活动时长
- **活动距离**: 显示今日移动距离
- **周/月统计**: 查看周、月活动趋势

#### 7.2 健康报告
- **活动报告**: 生成老人活动报告
- **安全报告**: 生成安全状况报告
- **周报/月报**: 定期生成周报、月报

#### 7.3 图表展示
- **活动趋势图**: 展示活动趋势变化
- **报警分布图**: 展示报警类型分布
- **位置热力图**: 展示常活动区域

### 8. 消息通知模块

#### 8.1 消息中心
- **消息列表**: 查看所有通知消息
- **消息分类**: 按类型分类（报警、系统、提醒）
- **消息详情**: 查看消息详细内容
- **消息操作**: 标记已读、删除消息

#### 8.2 推送设置
- **报警通知**: 开启/关闭报警推送
- **位置更新**: 开启/关闭位置更新推送
- **系统通知**: 开启/关闭系统通知
- **免打扰模式**: 设置免打扰时间段

### 9. 设置模块

#### 9.1 应用设置
- **刷新频率**: 设置位置刷新频率
- **地图模式**: 切换地图显示模式（标准、卫星）
- **语言设置**: 切换应用语言
- **主题设置**: 切换应用主题

#### 9.2 隐私设置
- **位置权限**: 管理位置权限
- **通知权限**: 管理通知权限
- **隐私政策**: 查看隐私政策
- **用户协议**: 查看用户协议

#### 9.3 关于我们
- **版本信息**: 显示应用版本号
- **帮助中心**: 查看使用帮助
- **联系客服**: 联系客服
- **意见反馈**: 提交意见反馈

## 页面结构设计

### 主要页面

```
小程序端页面结构
├── 首页 (Home)
│   ├── 实时位置地图
│   ├── 设备状态卡片
│   ├── 快捷功能入口
│   └── 最新报警信息
├── 监控页面 (Monitor)
│   ├── 实时监控
│   ├── 传感器数据
│   └── 设备详情
├── 轨迹页面 (Trajectory)
│   ├── 轨迹查询
│   ├── 轨迹回放
│   └── 轨迹统计
├── 报警页面 (Alarm)
│   ├── 报警列表
│   ├── 报警详情
│   └── 报警统计
├── 围栏页面 (Fence)
│   ├── 围栏列表
│   ├── 创建围栏
│   └── 围栏详情
├── 统计页面 (Statistics)
│   ├── 活动统计
│   ├── 健康报告
│   └── 数据图表
├── 消息页面 (Message)
│   ├── 消息列表
│   ├── 消息详情
│   └── 消息设置
├── 我的页面 (Profile)
│   ├── 个人信息
│   ├── 设备管理
│   ├── 老人管理
│   ├── 设置
│   └── 关于我们
└── 登录页面 (Login)
    ├── 手机号登录
    ├── 微信授权登录
    └── 注册
```

## API接口对接

### 认证接口

```
POST /api/mini/login          # 小程序登录
POST /api/mini/wechat-login   # 微信授权登录
POST /api/mini/register       # 监护人注册
POST /api/mini/logout         # 退出登录
GET  /api/mini/user/info     # 获取用户信息
PUT  /api/mini/user/info     # 更新用户信息
```

### 设备接口

```
GET  /api/mini/devices              # 获取设备列表
POST /api/mini/devices/bind         # 绑定设备
DELETE /api/mini/devices/{id}       # 解绑设备
GET  /api/mini/devices/{id}        # 获取设备详情
GET  /api/mini/devices/{id}/status # 获取设备状态
```

### 实时监控接口

```
GET /api/mini/devices/{id}/location     # 获取实时位置
GET /api/mini/devices/{id}/sensor-data # 获取传感器数据
GET /api/mini/devices/{id}/health     # 获取设备健康度
```

### 报警接口

```
GET  /api/mini/alarms              # 获取报警列表
GET  /api/mini/alarms/{id}         # 获取报警详情
PUT  /api/mini/alarms/{id}/handle  # 处理报警
GET  /api/mini/alarms/statistics   # 获取报警统计
```

### 围栏接口

```
GET  /api/mini/fences              # 获取围栏列表
POST /api/mini/fences              # 创建围栏
PUT  /api/mini/fences/{id}         # 更新围栏
DELETE /api/mini/fences/{id}       # 删除围栏
GET  /api/mini/fences/{id}/status  # 获取围栏状态
```

### 轨迹接口

```
GET /api/mini/devices/{id}/trajectory  # 获取轨迹数据
GET /api/mini/devices/{id}/statistics  # 获取轨迹统计
```

### 统计接口

```
GET /api/mini/devices/{id}/activity   # 获取活动统计
GET /api/mini/devices/{id}/report     # 获取健康报告
GET /api/mini/devices/{id}/charts     # 获取图表数据
```

### 消息接口

```
GET  /api/mini/messages          # 获取消息列表
PUT  /api/mini/messages/{id}/read # 标记消息已读
GET  /api/mini/messages/settings # 获取消息设置
PUT  /api/mini/messages/settings # 更新消息设置
```

## 数据模型

### 用户模型 (Guardian)

```javascript
{
  id: Number,              // 用户ID
  username: String,        // 用户名
  phone: String,          // 手机号
  nickname: String,       // 昵称
  avatar: String,         // 头像URL
  realName: String,       // 真实姓名
  idCard: String,         // 身份证号
  isVerified: Boolean,    // 是否实名认证
  createdAt: String,      // 注册时间
  updatedAt: String       // 更新时间
}
```

### 设备模型 (CaneDevice)

```javascript
{
  id: Number,              // 设备ID
  deviceId: String,       // 设备编号
  deviceName: String,     // 设备名称
  userId: Number,         // 所属老人ID
  userName: String,       // 老人姓名
  batteryLevel: Number,   // 电池电量 (0-100)
  status: String,         // 在线状态 (online/offline)
  healthScore: Number,    // 健康度评分 (0-100)
  lastDataTime: String,   // 最后数据时间
  createdAt: String,       // 创建时间
  updatedAt: String        // 更新时间
}
```

### 老人模型 (VisuallyImpairedUser)

```javascript
{
  id: Number,              // 用户ID
  name: String,           // 姓名
  age: Number,            // 年龄
  gender: String,         // 性别
  phone: String,          // 联系电话
  photo: String,          // 照片URL
  address: String,        // 家庭住址
  emergencyContact: String, // 紧急联系人
  emergencyPhone: String,   // 紧急联系电话
  createdAt: String,      // 创建时间
  updatedAt: String       // 更新时间
}
```

### 报警模型 (AlarmRecord)

```javascript
{
  id: Number,              // 报警ID
  deviceId: String,       // 设备ID
  deviceName: String,     // 设备名称
  alarmType: String,      // 报警类型
  alarmTime: String,      // 报警时间
  latitude: Number,       // 纬度
  longitude: Number,      // 经度
  address: String,        // 地址
  status: String,         // 状态 (pending/handled/ignored)
  handleTime: String,     // 处理时间
  handler: String,        // 处理人
  description: String,    // 描述
  createdAt: String,      // 创建时间
  updatedAt: String       // 更新时间
}
```

### 围栏模型 (ElectronicFence)

```javascript
{
  id: Number,              // 围栏ID
  fenceName: String,      // 围栏名称
  userId: Number,         // 所属老人ID
  userName: String,       // 老人姓名
  fenceType: String,      // 围栏类型 (circle/rectangle/polygon)
  coordinates: Array,      // 坐标点
  radius: Number,          // 半径（圆形围栏）
  isAlarmEnabled: Boolean, // 是否启用报警
  startTime: String,      // 生效开始时间
  endTime: String,        // 生效结束时间
  status: String,         // 状态 (active/inactive)
  createdAt: String,      // 创建时间
  updatedAt: String       // 更新时间
}
```

## 开发指南

### 环境搭建

#### 1. 微信小程序开发环境
- 下载微信开发者工具
- 注册微信小程序账号
- 获取AppID和AppSecret

#### 2. 项目初始化
```bash
# 使用微信开发者工具创建新项目
# 或使用Uni-app创建项目
npm install -g @vue/cli
vue create -p dcloudio/uni-preset-vue smart-cane-mini
```

#### 3. 安装依赖
```bash
cd smart-cane-mini
npm install
# 安装UI组件库
npm install vant-weapp
# 安装图表库
npm install echarts-for-weixin
```

### 开发规范

#### 1. 目录结构
```
smart-cane-mini/
├── pages/                  # 页面
│   ├── home/              # 首页
│   ├── monitor/           # 监控页
│   ├── trajectory/        # 轨迹页
│   ├── alarm/             # 报警页
│   ├── fence/             # 围栏页
│   ├── statistics/        # 统计页
│   ├── message/           # 消息页
│   ├── profile/           # 我的页
│   └── login/             # 登录页
├── components/            # 组件
├── utils/                 # 工具函数
├── api/                   # API接口
├── store/                 # 状态管理
├── styles/                # 样式文件
├── static/                # 静态资源
└── app.json               # 应用配置
```

#### 2. 代码规范
- 使用ES6+语法
- 组件命名采用PascalCase
- 文件命名采用kebab-case
- 代码注释清晰完整
- 遵循微信小程序开发规范

#### 3. 样式规范
- 使用rpx单位适配不同屏幕
- 统一使用Flex布局
- 颜色使用CSS变量
- 遵循设计稿规范

### 关键功能实现

#### 1. 地图集成
```javascript
// 使用微信小程序地图组件
<map
  id="map"
  longitude="{{longitude}}"
  latitude="{{latitude}}"
  scale="{{scale}}"
  markers="{{markers}}"
  polyline="{{polyline}}"
  show-location="{{true}}"
  bindmarkertap="onMarkerTap"
/>
```

#### 2. WebSocket连接
```javascript
// 建立WebSocket连接
const ws = wx.connectSocket({
  url: 'ws://localhost:8081/ws/alarm',
  header: {
    'Authorization': 'Bearer ' + token
  }
})

ws.onMessage((res) => {
  const data = JSON.parse(res.data)
  // 处理报警消息
  handleAlarm(data)
})
```

#### 3. 实时位置更新
```javascript
// 定时刷新位置
setInterval(() => {
  getLocation()
}, 30000) // 30秒刷新一次

function getLocation() {
  wx.request({
    url: 'http://localhost:8081/api/mini/devices/{id}/location',
    header: {
      'Authorization': 'Bearer ' + token
    },
    success: (res) => {
      this.setData({
        longitude: res.data.longitude,
        latitude: res.data.latitude
      })
    }
  })
}
```

#### 4. 轨迹回放
```javascript
// 轨迹回放动画
let currentIndex = 0
const trajectory = this.data.trajectory

function playTrajectory() {
  if (currentIndex >= trajectory.length) {
    currentIndex = 0
  }
  
  this.setData({
    polyline: [{
      points: trajectory.slice(0, currentIndex + 1),
      color: '#FF0000',
      width: 4
    }]
  })
  
  currentIndex++
  setTimeout(playTrajectory, 1000) // 1秒移动一个点
}
```

### 测试与部署

#### 1. 测试
- 功能测试：测试所有功能模块
- 兼容性测试：测试不同设备兼容性
- 性能测试：测试应用性能
- 用户体验测试：测试用户体验

#### 2. 部署
- 上传代码到微信后台
- 提交审核
- 发布上线

## 注意事项

### 1. 安全性
- 使用HTTPS加密传输
- Token加密存储
- 敏感信息加密
- 定期更新Token

### 2. 性能优化
- 图片懒加载
- 数据分页加载
- 合理使用缓存
- 减少网络请求

### 3. 用户体验
- 加载状态提示
- 错误友好提示
- 操作反馈及时
- 界面简洁易用

### 4. 兼容性
- 适配不同屏幕尺寸
- 兼容不同系统版本
- 处理网络异常
- 处理权限拒绝

### 5. 合规性
- 遵守微信小程序规范
- 保护用户隐私
- 获取必要权限
- 遵守相关法律法规

## 后续扩展

### 1. 功能扩展
- 视频通话功能
- 语音播报功能
- 社区功能
- 健康管理功能

### 2. 技术扩展
- 支持多端（支付宝小程序、百度小程序等）
- 引入AI智能分析
- 增加离线功能
- 优化推送服务

### 3. 数据扩展
- 数据分析报告
- 预测性分析
- 个性化推荐
- 数据可视化增强

## 联系方式

如有问题或建议，请联系项目维护者。
